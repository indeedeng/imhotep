#include <stdlib.h>
#include "imhotep_native.h"


int run_tgs_pass(struct session_desc *session, long *addresses, int *shard_handles, int num_shard, int socket_fd)
{
	packed_shard_t *shard;

	if ()

	return 0;
}
